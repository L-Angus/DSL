## 树和二叉树

### 1. 什么是树？什么是二叉树？

树：有层次关系N(N >= 0)个节点的有限集合。

- 空树：N = 0。

- 非空树：有且仅有一个根节点。

节点：根节点（没有前驱节点）、分支节点、叶子节点（没有后继节点）、前驱（父节点）、后继（子节点）。

子树：除根外，可分为m个互不相交的有限集合。

边、度、高度（深度、层次）。

节点数 = 总度数+1；

数的度为整个树形结构最大的度；